{
  "name": "cw-shim",
  "version": "1.0.0",
  "description": "ConnectWise API shim — logs x360Recover calls, returns mock CW responses",
  "scripts": {
    "build": "tsc",
    "start": "func start",
    "prestart": "npm run build"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0",
    "azure-functions-core-tools": "^4.0.0"
  }
}
