{
  "name": "cw-shim",
  "version": "1.0.0",
  "description": "ConnectWise API shim for x360Recover",
  "main": "dist/functions/*.js",
  "scripts": {
    "build": "tsc",
    "test": "node --test tests/**/*.test.js",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start"
  },
  "dependencies": {
    "@azure/functions": "^4.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
